<p style="color: green"><%= notice %></p>

<% content_for :title, "Users" %>

<h1>Users</h1>

<div id="users">
  <% @users.each do |user| %>
    <div id="<%= dom_id user %>">
      <div>
        <strong>Name:</strong>
        <%= user.name %>
      </div>
    </div>
  <% end %>
</div>